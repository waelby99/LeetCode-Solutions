## Palindrome Number

### Complexity Analysis

* **Time Complexity:** `O(n)`
  The solution uses loops the half of the string.
* **Space Complexity:** `O(n)`
  I create a string `str` that converts the whole number .  

### LeetCode Performance

- **Runtime:** `8 ms` — beats `8,27%`
- **Memory:** `45.89 MB` — beats `72.51%`